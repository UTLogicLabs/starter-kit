/**
 * Assumption (flag for design sign-off): the spec calls for "Liberation Mono
 * Bold" as the display font. We interpret that as headings/display text set
 * in the mono face at font-weight 700, with a normal humanist sans for body
 * copy so paragraphs stay readable.
 */
export const fontDisplay =
  '"Liberation Mono", "Courier New", Consolas, "SFMono-Regular", Menlo, monospace'

export const fontBody = 'Inter, "Helvetica Neue", Arial, "Segoe UI", sans-serif'
